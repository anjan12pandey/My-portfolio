name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger EC2 Deploy
        run: |
          curl -X POST http://3.27.92.92/deploy \
          -H "X-Deploy-Token: mysecret123"